var require = {
    //这里使用的是config.js的目录
        paths: {
            vue: './lib/vue.min',
            Zepto: './lib/zepto.min',
            Loading: './com/loading'
        },
        shim: {
        }
};